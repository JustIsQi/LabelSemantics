"""FastAPI inference service for LabelSemantics NER.

The actual ASGI app lives in :mod:`service.app`; the runner in
:mod:`service.__main__` lets you launch it with multiple uvicorn workers via
``python -m service``.
"""
